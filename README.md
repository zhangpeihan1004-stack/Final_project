# Final_project
My_final_version
The trained model is stored in the `results` file, and the dataset is stored in a `data` file within the `final_project` file. However, since both files are too large, I stored them in Google Drive. The link is: https://drive.google.com/drive/folders/1AIFIYOjKUHYH7VVjmu_R4kTaBrsJpcnI
在results文件中存训练好的模型，同时在final_project文件中还有一个data文件存储数据集，但由于这两个文件太大，所我存储到了googleDrive中，链接https://drive.google.com/drive/folders/1AIFIYOjKUHYH7VVjmu_R4kTaBrsJpcnI
The corresponding hardware configuration is in `run_train.sh`.
相应硬件配置在run_train.sh中
